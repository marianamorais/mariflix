import styled from 'styled-components';

const TagTitle = styled.h2`
  width: 20%;
  display: inline-block;
  border-radius: 4rem;
  font-size: 25rem;
  /* background-color: var(--color-tag-title); */
  background-image: linear-gradient(to right, var(--color-frontend) 0%, var(--color-frontend-light));
  padding: 20rem 18rem;
  text-align: center;
`;

export default TagTitle;